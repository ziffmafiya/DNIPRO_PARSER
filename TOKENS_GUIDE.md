# 🔑 Детальний гід по отриманню токенів

## 🤖 Telegram Bot Token

### Навіщо потрібен?
- Відправка сповіщень про нові графіки відключень
- Повідомлення про помилки в роботі парсера
- Статистика роботи програми

### Покрокова інструкція:

1. **Відкрийте Telegram** та знайдіть бота [@BotFather](https://t.me/BotFather)

2. **Створіть нового бота:**
   ```
   /newbot
   ```

3. **Введіть назву бота** (це те, що побачать користувачі):
   ```
   Dnipro Parser Bot
   ```

4. **Введіть username бота** (має закінчуватися на "bot"):
   ```
   dnipro_parser_bot
   ```

5. **Скопіюйте токен** з повідомлення BotFather:
   ```
   123456789:ABCdefGHIjklMNOpqrsTUVwxyz-Example
   ```

6. **Вставте токен** в файл `.env`:
   ```env
   BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz-Example
   ```

---

## 💬 Chat ID

### Навіщо потрібен?
- Визначає, куди бот буде відправляти повідомлення
- Може бути особистий чат або групова бесіда

### Покрокова інструкція:

1. **Додайте свого бота** в чат або напишіть йому особисто

2. **Відправте будь-яке повідомлення** боту (наприклад: "Привіт")

3. **Запустіть скрипт для отримання Chat ID:**
   ```bash
   python get_chat_id.py
   ```

4. **Скопіюйте Chat ID** з виводу скрипта та вставте в `.env`:
   ```env
   ADMIN_CHAT_ID=123456789
   ```

### Альтернативний спосіб (ручний):

1. **Відкрийте в браузері** (замініть YOUR_BOT_TOKEN на ваш токен):
   ```
   https://api.telegram.org/botYOUR_BOT_TOKEN/getUpdates
   ```

2. **Знайдіть в JSON відповіді** секцію з вашим повідомленням:
   ```json
   {
     "message": {
       "chat": {
         "id": 123456789,
         "type": "private"
       }
     }
   }
   ```

3. **Скопіюйте число з поля "id"** та вставте в `.env`

### Типи Chat ID:
- **Особистий чат:** позитивне число (123456789)
- **Групова бесіда:** негативне число (-123456789)
- **Супергрупа:** негативне число з 13 цифр (-1001234567890)

---

## 🐙 GitHub Token (опціонально)

### Навіщо потрібен?
- Автоматичне завантаження згенерованих зображень в GitHub
- Створення публічного архіву графіків
- Інтеграція з GitHub Pages для веб-перегляду

### Покрокова інструкція:

1. **Перейдіть на GitHub** → [Settings](https://github.com/settings/tokens)

2. **Натисніть "Generate new token"** → **"Generate new token (classic)"**

3. **Заповніть форму:**
   - **Note:** `DNIPRO_PARSER`
   - **Expiration:** `No expiration` (рекомендовано)

4. **Виберіть права доступу (scopes):**
   ```
   ✅ repo
     ✅ repo:status
     ✅ repo_deployment
     ✅ public_repo
     ✅ repo:invite
     ✅ security_events
   
   ✅ workflow (якщо використовуєте GitHub Actions)
   ```

5. **Натисніть "Generate token"**

6. **ВАЖЛИВО:** Скопіюйте токен одразу - він більше не відобразиться!
   ```
   ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   ```

7. **Вставте в `.env`:**
   ```env
   GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   GITHUB_REPO=your_username/DNIPRO_PARSER
   ```

---

## 🔒 Безпека токенів

### ⚠️ ВАЖЛИВО:
- **Ніколи не публікуйте токени** в коді або публічних репозиторіях
- **Використовуйте `.env` файл** який додано в `.gitignore`
- **Регулярно оновлюйте токени** (особливо GitHub)
- **Видаляйте невикористовувані токени**

### Що робити якщо токен скомпрометовано:

**Telegram Bot:**
1. Напишіть @BotFather
2. Виберіть свого бота
3. Натисніть "API Token" → "Revoke current token"

**GitHub Token:**
1. Перейдіть на [GitHub Settings](https://github.com/settings/tokens)
2. Знайдіть скомпрометований токен
3. Натисніть "Delete"
4. Створіть новий токен

---

## 🧪 Тестування налаштувань

### Перевірка Telegram бота:
```bash
# Відправте тестове повідомлення
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/sendMessage" \
     -d "chat_id=<YOUR_CHAT_ID>" \
     -d "text=Тест DNIPRO_PARSER бота"
```

### Перевірка GitHub токена:
```bash
# Перевірте доступ до репозиторію
curl -H "Authorization: token <YOUR_GITHUB_TOKEN>" \
     https://api.github.com/repos/<USERNAME>/<REPO>
```

---

## ❓ Часті питання

**Q: Чи можна використовувати парсер без Telegram бота?**
A: Ні, BOT_TOKEN обов'язковий для роботи програми.

**Q: Чи можна використовувати без GitHub токена?**
A: Так, GitHub токен опціональний. Просто залиште поле порожнім.

**Q: Як змінити чат для сповіщень?**
A: Змініть ADMIN_CHAT_ID в файлі `.env` та перезапустіть програму.

**Q: Бот не відправляє повідомлення в групу**
A: Переконайтеся, що бот додано в групу та має права на відправку повідомлень.

**Q: GitHub токен не працює**
A: Перевірте, чи вибрано правильні права доступу (scopes) при створенні токена.

---

## 📞 Підтримка

Якщо виникли проблеми з налаштуванням токенів:

1. Перевірте [Issues](https://github.com/ziffmafiya/DNIPRO_PARSER/issues)
2. Створіть новий Issue з детальним описом проблеми
3. Додайте логи з `logs/full_log.log` (без токенів!)

---

**Автор:** [ziffmafiya](https://github.com/ziffmafiya)  
**Оновлено:** Грудень 2025