# Настройка DNIPRO_PARSER

## Быстрый старт

1. **Клонируйте проект и установите зависимости:**
```bash
cd /home/yaroslav/bots/
git clone <your-repo> DNIPRO_PARSER
cd DNIPRO_PARSER
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

2. **Настройте переменные окружения:**
```bash
cp .env.example .env
nano .env
```
Заполните:
- `BOT_TOKEN` - токен вашего Telegram бота
- `ADMIN_CHAT_ID` - ID чата для уведомлений

3. **Создайте необходимые папки:**
```bash
mkdir -p out/images logs
```

4. **Тестовый запуск:**
```bash
python3 src/main.py --parse
```

## Автоматизация через systemd

Следуйте инструкциям в файле `Instruction.txt` для настройки автоматического запуска.

## Основные изменения от KHARKIV_PARSER

- **Источник данных:** @cek_info (ПрАТ "ПЕЕМ "ЦЕК", Днепр)
- **Регион:** Dnipro вместо Kharkiv  
- **Файл JSON:** Dneproblenergo.json
- **Формат сообщений:** 
  - Группы: "📌 X.Y черга:" вместо "GPV X.Y"
  - Интервалы: "з ЧЧ:ММ до ЧЧ:ММ" вместо "ЧЧ:ММ-ЧЧ:ММ"
  - Даты: "19 ГРУДНЯ" (заглавными буквами)
- **Ключевые слова:** добавлены "застосовуватимуться відключення наступних черг", "черга:"

## Особенности парсинга ЦЕК

Парсер адаптирован под специфический формат сообщений ЦЕК:
- Все данные находятся в одной строке (без переносов)
- Группы обозначаются как "X.Y черга" или "X.Y черги"
- Интервалы указываются в формате "з 00:00 до 02:30"
- Поддерживается переход через полночь (например, з 23:30 до 02:30)
- Убран сдвиг времени на +1 час (время указано корректно)

## Структура проекта

```
DNIPRO_PARSER/
├── src/
│   ├── main.py                    # Основной оркестратор
│   ├── dnipro_telegram_parser.py  # Парсер канала @cek_info
│   ├── gener_im_1_G.py           # Генератор изображений по группам
│   ├── gener_im_full.py          # Генератор общих графиков
│   ├── telegram_notify.py        # Уведомления в Telegram
│   ├── upload_to_github.py       # Загрузка в GitHub
│   └── utils.py                  # Утилиты
├── out/
│   ├── images/                   # Сгенерированные изображения
│   └── Dneproblenergo.json      # Данные отключений
├── logs/                         # Логи работы
├── .env                         # Переменные окружения
└── run_main.sh                  # Скрипт запуска
```