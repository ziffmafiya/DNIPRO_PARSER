# Налаштування DNIPRO_PARSER

## Швидкий старт

1. **Клонуйте проект та встановіть залежності:**
```bash
cd /home/yaroslav/bots/
git clone <your-repo> DNIPRO_PARSER
cd DNIPRO_PARSER
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

2. **Налаштуйте змінні оточення:**
```bash
cp .env.example .env
nano .env
```
Заповніть:
- `BOT_TOKEN` - токен вашого Telegram бота
- `ADMIN_CHAT_ID` - ID чату для сповіщень

3. **Створіть необхідні папки:**
```bash
mkdir -p out/images logs
```

4. **Тестовий запуск:**
```bash
python3 src/main.py --parse
```

## Автоматизація через systemd

Дотримуйтесь інструкцій у файлі `Instruction.txt` для налаштування автоматичного запуску.

## Основні зміни від KHARKIV_PARSER

- **Джерело даних:** @cek_info (ПрАТ "ПЕЕМ "ЦЕК", Дніпро)
- **Регіон:** Dnipro замість Kharkiv  
- **Файл JSON:** Dneproblenergo.json
- **Формат повідомлень:** 
  - Групи: "📌 X.Y черга:" замість "GPV X.Y"
  - Інтервали: "з ГГ:ХХ до ГГ:ХХ" замість "ГГ:ХХ-ГГ:ХХ"
  - Дати: "19 ГРУДНЯ" (великими літерами)
- **Ключові слова:** додано "застосовуватимуться відключення наступних черг", "черга:"

## Особливості парсингу ЦЕК

Парсер адаптований під специфічний формат повідомлень ЦЕК:
- Всі дані знаходяться в одному рядку (без переносів)
- Групи позначаються як "X.Y черга" або "X.Y черги"
- Інтервали вказуються у форматі "з 00:00 до 02:30"
- Підтримується перехід через північ (наприклад, з 23:30 до 02:30)
- Прибрано зсув часу на +1 годину (час вказано коректно)

## Структура проекту

```
DNIPRO_PARSER/
├── src/
│   ├── main.py                    # Основний оркестратор
│   ├── dnipro_telegram_parser.py  # Парсер каналу @cek_info
│   ├── gener_im_1_G.py           # Генератор зображень по групах
│   ├── gener_im_full.py          # Генератор загальних графіків
│   ├── telegram_notify.py        # Сповіщення в Telegram
│   ├── upload_to_github.py       # Завантаження в GitHub
│   └── utils.py                  # Утиліти
├── out/
│   ├── images/                   # Згенеровані зображення
│   └── Dneproblenergo.json      # Дані відключень
├── logs/                         # Логи роботи
├── .env                         # Змінні оточення
└── run_main.sh                  # Скрипт запуску
```