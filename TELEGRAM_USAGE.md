# 📱 Використання Telegram бота

## 🚀 Швидкий старт

### 1. Налаштування бота
```bash
# 1. Отримайте Chat ID
python get_chat_id.py

# 2. Додайте Chat ID в .env файл
ADMIN_CHAT_ID=ваш_chat_id
```

### 2. Відправка графіків

**Швидка відправка (рекомендовано):**
```bash
python quick_send.py
```

**Детальне управління:**
```bash
python send_schedule.py
```

---

## 📋 Доступні команди

### 🔥 Основні команди

**Відправити всі графіки:**
```bash
python send_schedule.py all
```

**Відправити тільки статистику:**
```bash
python send_schedule.py stats
```

**Відправити графік групи:**
```bash
python send_schedule.py group 1-1
python send_schedule.py group 2-2
```

**Показати доступні зображення:**
```bash
python send_schedule.py list
```

### 🎯 Інтерактивне меню
```bash
python send_schedule.py
```
Відкриє меню з вибором дій.

---

## 📊 Що відправляється

### 1. Загальний графік (`all`)
- 🖼️ Зображення з усіма групами
- 📊 Статистика дня
- ⏰ Наступне відключення
- 📈 Найдовше відключення

### 2. Графік групи (`group X-Y`)
- 🖼️ Детальний графік для групи
- 📅 Дані на 2 дні
- 🎨 Покращена легенда

### 3. Тільки статистика (`stats`)
- ⚡ Години зі світлом
- 🔌 Години відключень
- 📈 Найдовше відключення
- ⏰ Наступне відключення

---

## 🛠️ Налаштування повідомлень

### Формат повідомлень
Всі повідомлення використовують HTML розмітку:
- `<b>Жирний текст</b>`
- Емодзі для кращого сприйняття
- Структурована інформація

### Звукові сповіщення
- 🔔 **Увімкнено** для важливих повідомлень
- 🔕 **Вимкнено** для звичайних повідомлень

---

## 📁 Структура файлів

```
DNIPRO_PARSER/
├── get_chat_id.py          # Отримання Chat ID
├── send_schedule.py        # Детальне управління
├── quick_send.py           # Швидка відправка
└── out/images/
    ├── gpv-all-today.png   # Загальний графік
    ├── gpv-1-1-emergency.png  # Графік групи 1.1
    └── ...
```

---

## 🔧 Автоматизація

### Щоденна відправка
Додайте в cron (Linux) або Task Scheduler (Windows):

```bash
# Щодня о 08:00
0 8 * * * cd /path/to/DNIPRO_PARSER && python quick_send.py
```

### Відправка після парсингу
Додайте в кінець `src/main.py`:

```python
# Автоматична відправка після генерації
from pathlib import Path
import subprocess

try:
    subprocess.run([sys.executable, "quick_send.py"], check=True)
except Exception as e:
    log(f"Помилка відправки в Telegram: {e}")
```

---

## ❓ Часті питання

**Q: Бот не відправляє повідомлення**
A: Перевірте:
1. BOT_TOKEN правильний
2. ADMIN_CHAT_ID правильний
3. Бот не заблокований

**Q: Як відправити в групу?**
A: 
1. Додайте бота в групу
2. Отримайте Chat ID групи (буде негативним)
3. Використовуйте цей ID в .env

**Q: Як змінити формат повідомлень?**
A: Відредагуйте функції в `src/telegram_notify.py`

**Q: Можна відправляти в кілька чатів?**
A: Так, змініть `send_photo()` для відправки в список чатів

---

## 🎨 Приклади повідомлень

### Загальний графік
```
🔌 График отключений электроэнергии
📍 Дніпро • ЦЕК
⏰ Актуальная информация

📊 Статистика на 20 грудня:
⚡ Світло є: 10.7 годин (в середньому)
🔌 Відключення: 13.3 годин (в середньому)
📈 Найдовше відключення: 8 годин (13:00-21:00)
   Група: 1.1

⏰ Наступне відключення:
🔴 Через 2 години 15 хвилин
⏱️ З 20:00 до 23:30 (3.5 години)
   Група: 1.1
```

### Графік групи
```
📊 График отключений - Группа 1-1
Детальный график на 2 дня
```

---

## 🚀 Розширені можливості

### Кастомні повідомлення
```python
from src.telegram_notify import send_message

send_message("🔥 Термінове повідомлення!", urgent=True)
```

### Відправка файлів
```python
from src.telegram_notify import send_photo

send_photo("path/to/image.png", "Підпис до зображення")
```

### Тільки статистика
```python
from src.telegram_notify import send_stats_only

send_stats_only()
```

---

**Автор:** [ziffmafiya](https://github.com/ziffmafiya)  
**Оновлено:** Грудень 2025