# 🔄 Руководство по переходу на HTML Renderer

## ✅ Что уже готово

Новая система HTML рендерера успешно установлена и протестирована:

- ✅ **78 изображений** сгенерировано (39 светлых + 39 темных)
- ✅ **5 типов графиков**: полный, аварийный, недельный, матрица групп, карточка
- ✅ **12 GPV групп**: от GPV1.1 до GPV6.2
- ✅ **Обе темы**: светлая и темная
- ✅ **Высокое качество**: 1766x556 до 1766x1376 пикселей

## 🚀 Как использовать новую систему

### 1. Генерация всех изображений
```bash
# Генерирует все типы изображений для всех групп (светлая + темная тема)
python generate_all_images.py
```

### 2. Быстрое тестирование
```bash
# Генерирует несколько тестовых изображений
python test_html_renderer.py
```

### 3. Отправка в Telegram
```bash
# Все графики (светлые)
python send_schedule.py all

# Все графики (темные)
python send_schedule.py all --dark

# Конкретная группа
python send_schedule.py group 1-1

# Конкретная группа (темная)
python send_schedule.py group 1-1 --dark

# Только статистика
python send_schedule.py stats

# Список всех изображений
python send_schedule.py list
```

## 📊 Типы генерируемых изображений

### 1. **Полный график** (`gpv-full-*.png`)
- Размер: ~200 КБ
- Содержит: таблица сегодня/завтра + недельная матрица
- Лучший для общего обзора

### 2. **Матрица групп** (`gpv-all-groups-*.png`)
- Размер: ~200 КБ  
- Содержит: все группы на одном изображении
- Варианты: сегодня и завтра

### 3. **Аварийный график** (`gpv-{группа}-emergency-*.png`)
- Размер: ~100 КБ
- Содержит: детальный график для конкретной группы на 2 дня
- Лучший для отправки по группам

### 4. **Недельный график** (`gpv-{группа}-week-*.png`)
- Размер: ~150 КБ
- Содержит: прогноз на неделю для группы
- Показывает тенденции

### 5. **Карточка** (`gpv-{группа}-summary-*.png`)
- Размер: ~50 КБ
- Содержит: компактная информация с интервалами
- Лучший для быстрого просмотра

## 🎨 Преимущества новой системы

### ✅ Качество
- **Профессиональный дизайн** через HTML/CSS
- **Высокое разрешение** через Chromium рендеринг
- **Четкие шрифты** и иконки
- **Адаптивная верстка**

### ✅ Функциональность
- **Темная тема** для ночного режима
- **SVG иконки** для состояний отключений
- **Украинская локализация** дат и названий
- **Цветовая кодировка** состояний

### ✅ Гибкость
- **Легкое изменение дизайна** через CSS
- **Быстрое добавление новых типов** графиков
- **Настраиваемые темы** и цвета
- **Масштабируемость** для других городов

## 🔧 Настройка

### Изменение цветов
Отредактируйте `исходники/schedule-shared.css`:
```css
:root {
  --bg: #f6f8fb;           /* Фон светлой темы */
  --text: #0e1624;         /* Текст светлой темы */
  --accent: #0b6bcb;       /* Акценты светлой темы */
}

.theme-dark {
  --bg: #0b0d12;           /* Фон темной темы */
  --text: #e8ecf1;         /* Текст темной темы */
  --accent: #5bbcff;       /* Акценты темной темы */
}
```

### Изменение качества
В `src/html_renderer.py` измените параметр `scale`:
```python
await self._render_template(
    template_name,
    output_path,
    scale=2.0  # 1.0 = обычное, 2.0 = высокое, 3.0 = максимальное
)
```

## 📈 Производительность

### Время генерации
- **Одно изображение**: ~2-3 секунды
- **Все изображения**: ~3-4 минуты (78 изображений)
- **Только светлая тема**: ~1.5-2 минуты

### Размеры файлов
- **Полные графики**: 150-250 КБ
- **Аварийные графики**: 80-120 КБ
- **Карточки**: 30-60 КБ
- **Общий размер**: ~10-15 МБ для всех изображений

## 🔄 Автоматизация

### Обновление основного скрипта
Замените в `src/main.py` вызовы старых генераторов:
```python
# Старый код (удалить)
# run_generator("gener_im_1_G.py")
# run_generator("gener_im_full.py") 
# run_generator("gener_im_dark.py")

# Новый код (добавить)
import asyncio
from html_renderer import HTMLRenderer

async def generate_images():
    json_path = find_latest_json()
    renderer = HTMLRenderer(json_path)
    try:
        await renderer.generate_all_images("light")
        await renderer.generate_all_images("dark")
    finally:
        renderer.cleanup_temp()

# В main() функции
asyncio.run(generate_images())
```

### Systemd сервис (Linux)
Обновите `/etc/systemd/system/dnipro-parser.service`:
```ini
[Unit]
Description=Dnipro Parser HTML Renderer
After=network.target

[Service]
Type=oneshot
User=your_user
WorkingDirectory=/path/to/DNIPRO_PARSER
ExecStart=/usr/bin/python3 generate_all_images.py
Environment=PATH=/usr/bin:/usr/local/bin

[Install]
WantedBy=multi-user.target
```

### Cron задача
```bash
# Каждые 2 часа
0 */2 * * * cd /path/to/DNIPRO_PARSER && python3 generate_all_images.py
```

## 🐛 Устранение проблем

### Ошибка "Нет доступных GPV групп"
- Проверьте JSON файл: `cat out/Dneproblenergo.json | grep GPV`
- Убедитесь, что есть данные в `fact.data`

### Ошибка Playwright
```bash
# Переустановите Chromium
python -m playwright install chromium
```

### Проблемы с шаблонами
- Проверьте папку `исходники/`
- Убедитесь, что все HTML, CSS, JS и SVG файлы на месте

### Медленная генерация
- Уменьшите `scale` до 1.5 или 1.0
- Закройте другие приложения для освобождения памяти

## 🎯 Результат

После перехода на HTML рендерер вы получили:

- 🎨 **Профессиональные графики** вместо простых таблиц
- 🌙 **Темную тему** для комфортного просмотра
- ⚡ **Быструю разработку** новых типов графиков
- 📱 **Качественные изображения** для Telegram
- 🔧 **Легкую настройку** дизайна через CSS

---

**Поздравляем! Ваш DNIPRO_PARSER теперь использует современную HTML-based систему генерации изображений! 🎉**