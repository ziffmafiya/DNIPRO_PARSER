# 🎨 HTML Renderer для DNIPRO_PARSER

Новая система генерации изображений через HTML/CSS шаблоны вместо Python/Pillow.

## 🚀 Преимущества нового подхода

### ✅ Качество и гибкость
- **Высокое качество** рендеринга через Chromium
- **Профессиональный дизайн** с CSS стилями
- **Темная и светлая темы** через CSS переменные
- **Адаптивная верстка** с responsive дизайном

### ✅ Простота разработки
- **HTML/CSS/JS** вместо сложного рисования в Pillow
- **Легкое изменение дизайна** через CSS
- **Быстрое добавление новых типов** графиков
- **Отладка в браузере** для разработки

### ✅ Функциональность
- **5 типов изображений**: полный, аварийный, недельный, матрица групп, карточка
- **2 темы**: светлая и темная
- **Автоматическая локализация** (украинские даты)
- **Иконки SVG** для состояний отключений

## 📁 Структура файлов

```
DNIPRO_PARSER/
├── исходники/                    # HTML шаблоны и ресурсы
│   ├── full-template.html        # Полный график (сегодня + неделя)
│   ├── emergency-template.html   # Аварийный график (сегодня/завтра)
│   ├── week-template.html        # Недельный график
│   ├── groups-template.html      # Матрица всех групп
│   ├── summary-item.html         # Компактная карточка
│   ├── schedule-shared.css       # Общие стили
│   ├── schedule-shared.js        # JavaScript логика
│   └── *.svg                     # Иконки состояний
├── src/
│   └── html_renderer.py          # Новый HTML рендерер
├── generate_all_images.py        # Обновленный генератор
├── test_html_renderer.py         # Тестовый скрипт
└── send_schedule.py              # Обновленная отправка
```

## 🛠️ Установка и настройка

### 1. Установка зависимостей
```bash
pip install playwright python-dotenv
playwright install chromium
```

### 2. Проверка шаблонов
Убедитесь, что папка `исходники` содержит все необходимые файлы:
- ✅ HTML шаблоны (5 файлов)
- ✅ CSS стили (`schedule-shared.css`)
- ✅ JavaScript (`schedule-shared.js`)
- ✅ SVG иконки (`*.svg`)

### 3. Тестирование
```bash
# Быстрый тест одного изображения
python test_html_renderer.py

# Генерация всех изображений
python generate_all_images.py
```

## 🎯 Использование

### Генерация изображений
```bash
# Все изображения (светлая + темная тема)
python generate_all_images.py

# Тестирование отдельных типов
python test_html_renderer.py
```

### Отправка в Telegram
```bash
# Все графики (светлые)
python send_schedule.py all

# Все графики (темные)
python send_schedule.py all --dark

# Конкретная группа
python send_schedule.py group 1-1

# Только статистика
python send_schedule.py stats

# Список доступных изображений
python send_schedule.py list
```

## 🖼️ Типы генерируемых изображений

### 1. **Полный график** (`gpv-full-*.png`)
- Таблица сегодня/завтра
- Недельная матрица
- Легенда с иконками
- Статистика обновлений

### 2. **Аварийный график** (`gpv-{группа}-emergency-*.png`)
- Детальный график для конкретной группы
- Сегодня и завтра
- Половинчатые часы (first/second)

### 3. **Недельный график** (`gpv-{группа}-week-*.png`)
- Прогноз на неделю
- Выделение текущего дня
- Цветовая кодировка состояний

### 4. **Матрица групп** (`gpv-all-groups-*.png`)
- Все группы на одном изображении
- Сегодня или завтра
- Компактное представление

### 5. **Карточка** (`gpv-{группа}-summary-*.png`)
- Компактная карточка для группы
- Интервалы отключений
- ON/OFF статус

## 🎨 Темы

### ☀️ Светлая тема
- Белый фон (`#f6f8fb`)
- Темный текст (`#0e1624`)
- Синие акценты (`#0b6bcb`)

### 🌙 Темная тема
- Темный фон (`#0b0d12`)
- Светлый текст (`#e8ecf1`)
- Голубые акценты (`#5bbcff`)

## 🔧 Настройка дизайна

### Изменение цветов
Отредактируйте CSS переменные в `исходники/schedule-shared.css`:
```css
:root {
  --bg: #f6f8fb;           /* Фон */
  --text: #0e1624;         /* Текст */
  --accent: #0b6bcb;       /* Акценты */
}

.theme-dark {
  --bg: #0b0d12;           /* Темный фон */
  --text: #e8ecf1;         /* Светлый текст */
}
```

### Добавление нового типа изображения
1. Создайте HTML шаблон в `исходники/`
2. Добавьте метод в `HTMLRenderer`
3. Обновите `generate_all_images.py`

### Изменение размеров
Настройте параметры в `html_renderer.py`:
```python
context = await browser.new_context(
    device_scale_factor=2.0,    # Качество (DPI)
    viewport={'width': 1200, 'height': 800}  # Размер окна
)
```

## 🐛 Отладка

### Проблемы с рендерингом
1. Проверьте логи в `logs/full_log.log`
2. Убедитесь, что Chromium установлен: `playwright install chromium`
3. Проверьте права доступа к папкам

### Проблемы с шаблонами
1. Откройте HTML файл в браузере напрямую
2. Проверьте консоль браузера на ошибки JavaScript
3. Убедитесь, что все ресурсы (CSS, JS, SVG) доступны

### Проблемы с данными
1. Проверьте формат JSON файла
2. Убедитесь, что есть данные для нужных GPV групп
3. Проверьте структуру данных в `preset` и `fact`

## 📊 Производительность

### Время генерации
- **Одно изображение**: ~2-3 секунды
- **Все изображения**: ~30-60 секунд (зависит от количества групп)
- **Темная + светлая тема**: ~1-2 минуты

### Размеры файлов
- **Полный график**: ~200-400 КБ
- **Аварийный график**: ~150-300 КБ
- **Карточка**: ~50-100 КБ

### Оптимизация
- Используйте `scale=1.5` для меньших файлов
- Используйте `scale=3.0` для максимального качества
- Очищайте старые изображения регулярно

## 🔄 Миграция со старой системы

### Что изменилось
- ❌ Старые генераторы: `gener_im_1_G.py`, `gener_im_full.py`, `gener_im_dark.py`
- ✅ Новый генератор: `html_renderer.py`
- ✅ Обновленный: `generate_all_images.py`
- ✅ Обновленный: `send_schedule.py`

### Совместимость
- Новые имена файлов: `gpv-{группа}-{тип}-{тема}-{время}.png`
- Старые скрипты будут работать, но рекомендуется обновление
- JSON формат остается тем же

### Переход
1. Установите Playwright: `pip install playwright && playwright install chromium`
2. Проверьте папку `исходники` с шаблонами
3. Запустите `python test_html_renderer.py`
4. Обновите автоматизацию на `python generate_all_images.py`

## 🎉 Результат

После внедрения HTML рендерера вы получите:
- 🎨 **Профессиональный дизайн** графиков
- 🌙 **Темную тему** для ночного режима
- ⚡ **Быструю разработку** новых типов
- 🔧 **Легкую настройку** через CSS
- 📱 **Качественные изображения** для Telegram

---

**Автор:** [ziffmafiya](https://github.com/ziffmafiya)  
**Версия:** 2.0.0 (HTML Renderer)  
**Дата:** Декабрь 2025