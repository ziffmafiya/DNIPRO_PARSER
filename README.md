# 🔌 DNIPRO_PARSER

**Парсер графіків відключень електроенергії для Дніпра**

Автоматично отримує дані з Telegram каналу [@cek_info](https://t.me/cek_info), генерує зображення графіків через HTML/CSS рендеринг та відправляє їх у ваш Telegram канал.

## 🎯 Основні функції

- 📡 **Автоматичний парсинг** з Telegram каналу @cek_info
- 🖼️ **HTML/CSS генерація зображень** високої якості
- 📊 **5 типів графіків**: повний, аварійний, тижневий, матриця груп, картки
- 📱 **Telegram бот** для відправки повідомлень
- 🔄 **Автоматичне оновлення** через systemd
- 📈 **Логування** з ротацією файлів
- 🌐 **Playwright рендеринг** для професійних зображень

## ⚡ Швидкий старт

### 1. Встановлення залежностей
```bash
# Автоматична установка
make setup

# Або вручную
pip install -r requirements.txt
playwright install chromium
```

### 2. Налаштування токенів
Створіть файл `.env` на основі `.env.example`:
```bash
cp .env.example .env
```

Заповніть необхідні токени:
```env
# Telegram бот
BOT_TOKEN=your_bot_token_here
ADMIN_CHAT_ID=your_chat_id_here

# GitHub (опціонально)
GITHUB_TOKEN=your_github_token_here
```

### 3. Повний цикл (парсинг + генерація)
```bash
# Рекомендований спосіб - все в одній команді
make run
# або
./run.sh full
# або
python -m src.main --parse
```

### 4. Або окремими кроками
```bash
# Парсинг даних
make parse

# Генерація всіх зображень
make generate

# Відправка в Telegram
make send
```

## 📋 Структура проекту

```
DNIPRO_PARSER/
├── .github/                   # GitHub Actions
│   └── workflows/
│       └── test.yml
├── config/                    # Настройки (если их много)
│   └── settings.yaml          # (Опционально) вместо хардкода
├── docs/                      # Вся документация
│   ├── CONTRIBUTING.md
│   ├── HTML_RENDERER_README.md
│   ├── MIGRATION_GUIDE.md
│   └── RELEASE_NOTES.md
├── logs/                      # Логи (пустая, но с .gitkeep)
├── output/                    # Результаты работы
│   ├── images/                # Згенеровані зображення
│   └── *.json                 # JSON дані
├── scripts/                   # Вспомогательные скрипты
│   ├── quick_send.py
│   ├── get_chat_id.py
│   └── setup_html_renderer.py
├── src/                       # Основной код (Application Logic)
│   ├── __init__.py            # Делает папку Python-пакетом
│   ├── main.py                # ЕДИНАЯ точка входа (CLI)
│   ├── config.py              # Загрузка .env и переменных
│   ├── html_renderer.py       # Логика рендера
│   ├── telegram_notify.py     # Уведомления и парсинг
│   ├── dnipro_telegram_parser.py # Парсер Telegram
│   ├── logger.py              # Настройка логгера
│   └── utils.py               # Утилиты
├── templates/                 # HTML шаблоны и ресурсы
│   ├── assets/                # *.svg іконки
│   ├── css/                   # *.css стилі
│   ├── js/                    # *.js скрипти
│   ├── full-template.html     # Повний графік
│   ├── emergency-template.html # Аварійний графік
│   ├── week-template.html     # Тижневий графік
│   ├── groups-template.html   # Матриця груп
│   └── summary-item.html      # Компактні картки
├── tests/                     # Тесты
│   ├── test_html_renderer.py
│   └── test_centering.py
├── .env.example               # Пример переменных окружения
├── .gitignore                 # Правильный игнор-лист
├── LICENSE
├── Makefile                   # Короткие команды типа "make run"
├── README.md                  # Главная страница
├── requirements.txt           # Зависимости
├── run.sh                     # Единый скрипт запуска
└── VERSION                    # Версия проекта
```

## 🎨 Типи зображень (HTML рендеринг)

### 📊 5 типів графіків
1. **Повний графік** - сьогодні/завтра + тижневий прогноз
2. **Аварійний графік** - детальний на 2 дні для конкретної групи
3. **Тижневий графік** - розклад на тиждень для групи
4. **Матриця груп** - всі групи на сьогодні/завтра
5. **Компактні картки** - короткий опис для групи

### ✨ Переваги HTML рендерингу
- 🎯 **Висока якість** - векторна графіка та чіткий текст
- 🎨 **Професійний дизайн** - CSS стилізація
- � * *Адаптивність** - оптимізація під різні розміри
- 🔧 **Легке налаштування** - зміна стилів через CSS
- ⚡ **Швидкість** - Playwright рендеринг

### 📊 Статистика
Кожне повідомлення містить:
- Загальну статистику дня
- Час до наступного відключення
- Тривалість відключень
- Найдовше відключення дня

## 🔧 Команди

### Основний процес (рекомендовано)
```bash
# Повний цикл: парсинг + генерація HTML зображень
make run
# або
./run.sh full
# або
python -m src.main --parse
```

### Makefile команди (рекомендовано)
```bash
make help          # Показати всі доступні команди
make setup         # Первоначальная настройка
make run           # Полный цикл
make parse         # Только парсинг
make generate      # Только генерация
make send          # Отправка в Telegram
make test          # Запуск тестов
make clean         # Очистка временных файлов
make status        # Статус проекта
```

### Bash скрипт
```bash
./run.sh help      # Справка
./run.sh setup     # Настройка
./run.sh full      # Полный цикл
./run.sh parse     # Парсинг
./run.sh generate  # Генерация
./run.sh send      # Отправка
./run.sh test      # Тесты
```

### Генерація зображень (HTML система)
```bash
# Всі типи зображень одразу
python generate_all_images.py

# Тестування HTML рендерера
python tests/test_html_renderer.py

# Тестування центрування тексту
python tests/test_centering.py
```

### Telegram
```bash
# Відправка
python send_schedule.py all              # Всі графіки
python send_schedule.py group 1-1        # Конкретна група
python send_schedule.py stats            # Тільки статистика

# Допоміжні команди
python send_schedule.py list             # Список зображень
python scripts/get_chat_id.py            # Отримати Chat ID
python scripts/quick_send.py             # Швидка відправка
```

### Налаштування HTML рендерера
```bash
# Встановлення Playwright
python scripts/setup_html_renderer.py
```

### Очистка старых изображений
```bash
# Автоматическая очистка (удаляет изображения старше 3 дней)
python scripts/cleanup_old_images.py

# Тестовый режим - показать что будет удалено
python scripts/cleanup_old_images.py --dry-run

# Сохранить только за последние 5 дней
python scripts/cleanup_old_images.py --days 5

# Очистить другую папку
python scripts/cleanup_old_images.py --dir custom/images/
```

## 📱 Telegram інтеграція

### Налаштування бота
1. Створіть бота через [@BotFather](https://t.me/BotFather)
2. Отримайте токен бота
3. Знайдіть Chat ID вашого каналу:
   ```bash
   python get_chat_id.py
   ```
4. Додайте токени в `.env` файл

### Відправка повідомлень
```bash
# Інтерактивне меню
python send_schedule.py

# Швидка відправка останнього графіку
python quick_send.py
```

## 🔄 Автоматизація

### Linux (systemd)
```bash
# Копіювати сервіс
sudo cp dnipro-parser.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable dnipro-parser
sudo systemctl start dnipro-parser
```

### Windows (Task Scheduler)
Створіть завдання для запуску `run_main.sh` кожні 30 хвилин.

## 📊 Моніторинг

### Логи
```bash
# Переглянути логи
tail -f logs/full_log.log

# Логи systemd (Linux)
journalctl -u dnipro-parser -f
```

### Статус
```bash
# Перевірити статус сервісу
sudo systemctl status dnipro-parser

# Переглянути згенеровані зображення
python send_schedule.py list
```

## 🛠️ Розробка

### Встановлення для розробки
```bash
git clone https://github.com/ziffmafiya/DNIPRO_PARSER.git
cd DNIPRO_PARSER
pip install -r requirements.txt
cp .env.example .env
# Заповніть .env файл
```

### Тестування
```bash
# Тест парсингу
python src/dnipro_telegram_parser.py

# Тест HTML генерації
python generate_all_images.py
python test_html_renderer.py

# Тест Telegram
python send_schedule.py stats
```

### Структура коду
- `src/main.py` - головний цикл (оновлений для HTML)
- `src/html_renderer.py` - HTML/CSS рендерер зображень
- `src/dnipro_telegram_parser.py` - парсинг @cek_info
- `исходники/` - HTML шаблони та CSS стилі
- `src/telegram_notify.py` - Telegram API
- `src/logger.py` - система логування

## 📚 Документація

- [docs/CONTRIBUTING.md](docs/CONTRIBUTING.md) - Руководство для контрибюторов
- [docs/HTML_RENDERER_README.md](docs/HTML_RENDERER_README.md) - HTML рендеринг система
- [docs/MIGRATION_GUIDE.md](docs/MIGRATION_GUIDE.md) - Міграція на HTML систему
- [docs/RELEASE_NOTES.md](docs/RELEASE_NOTES.md) - История версий

## 🎯 Версії

### v2.0.0 (Поточна - HTML система)
- ✅ **HTML/CSS рендеринг** замість Pillow
- ✅ **5 типів зображень** (повний, аварійний, тижневий, матриця, картки)
- ✅ **Playwright рендеринг** для високої якості
- ✅ **Українські коментарі** у всьому коді
- ✅ **Покращена архітектура** та організація коду
- ❌ **Видалена темна тема** (спрощення)

### v1.3.0
- ✅ Темна тема зображень
- ✅ Покращена статистика
- ✅ Автоматична генерація всіх типів
- ✅ Оновлений Telegram інтерфейс
- ❌ Видалена мобільна версія (погана якість)

### v1.2.0
- ✅ Статистика в повідомленнях
- ✅ Прогноз наступних відключень
- ✅ Дата в назвах файлів
- ✅ Покращена легенда
- ✅ Ротація логів
- ✅ Звукові сповіщення

### v1.1.0
- ✅ Базовий парсинг @cek_info
- ✅ Генерація зображень
- ✅ Telegram інтеграція
- ✅ Systemd сервіс

## 🤝 Внесок

1. Fork проекту
2. Створіть feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit зміни (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Відкрийте Pull Request

## 📄 Ліцензія

Цей проект ліцензовано під MIT License - дивіться [LICENSE](LICENSE) файл для деталей.

## 🙏 Подяки

- Telegram каналу [@cek_info](https://t.me/cek_info) за надання даних
- Спільноті Дніпра за підтримку проекту
- Всім контрибюторам за покращення коду

## 📞 Контакти

- **GitHub**: [ziffmafiya](https://github.com/ziffmafiya)
- **Telegram**: [@cek_info](https://t.me/cek_info)
- **Issues**: [GitHub Issues](https://github.com/ziffmafiya/DNIPRO_PARSER/issues)

---

**⚡ Слава Україні! ⚡**