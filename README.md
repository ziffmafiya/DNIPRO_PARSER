# DNIPRO_PARSER

🔌 **Парсер графиков отключений электроэнергии для Днепра**

Автоматический парсер сообщений от Telegram канала [@cek_info](https://t.me/cek_info) с генерацией наглядных графиков отключений электроэнергии для города Днепр.

## 🚀 Возможности

- **Автоматический парсинг** сообщений от ЦЕК Днепра
- **Генерация JSON** с данными отключений по группам
- **Создание изображений** с графиками отключений
- **Telegram уведомления** о новых графиках
- **Загрузка на GitHub** для публичного доступа
- **Поддержка получасовых интервалов** (first/second/mfirst/msecond)

## 📋 Требования

- Python 3.8+
- Telegram Bot Token
- Системные шрифты (Segoe UI для Windows, DejaVu Sans для Linux)

## 🛠️ Установка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/ziffmafiya/DNIPRO_PARSER.git
cd DNIPRO_PARSER
```

2. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

3. **Настройте переменные окружения:**
```bash
cp .env.example .env
# Отредактируйте .env файл с вашими данными
```

4. **Запустите парсер:**
```bash
python src/main.py --parse
```

## ⚙️ Конфигурация

Создайте файл `.env` на основе `.env.example`:

```env
BOT_TOKEN=your_telegram_bot_token
ADMIN_CHAT_ID=your_chat_id
GITHUB_TOKEN=your_github_token
GITHUB_REPO=your_username/your_repo
```

## 📊 Структура проекта

```
DNIPRO_PARSER/
├── src/
│   ├── main.py                    # Основной скрипт
│   ├── dnipro_telegram_parser.py  # Парсер сообщений ЦЕК
│   ├── gener_im_full.py          # Генератор полных графиков
│   ├── gener_im_1_G.py           # Генератор графиков по группам
│   ├── telegram_notify.py        # Telegram уведомления
│   ├── upload_to_github.py       # Загрузка на GitHub
│   └── utils.py                  # Утилиты
├── out/
│   └── images/                   # Сгенерированные изображения
├── logs/                         # Логи работы
├── .env.example                  # Пример конфигурации
├── requirements.txt              # Python зависимости
├── run_main.sh                   # Скрипт запуска
├── SETUP.md                      # Инструкции по настройке
└── Instruction.txt               # Детальные инструкции
```

## 🖼️ Примеры изображений

Парсер генерирует два типа изображений:

1. **Полный график** (`gpv-all-today.png`) - все группы на одном изображении
2. **Графики по группам** (`gpv-X-Y-emergency.png`) - детальный график для каждой группы

## 🔄 Автоматизация

Для автоматического запуска используйте systemd (Linux) или Task Scheduler (Windows).

Пример systemd сервиса в `Instruction.txt`.

## 📝 Логирование

Все операции логируются в `logs/full_log.log` с временными метками.

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## 🆘 Поддержка

Если у вас есть вопросы или проблемы:

1. Проверьте [Issues](https://github.com/ziffmafiya/DNIPRO_PARSER/issues)
2. Создайте новый Issue с описанием проблемы
3. Приложите логи из `logs/full_log.log`

## 🏷️ Версии

- **v1.0.0** - Базовая функциональность парсинга и генерации изображений
- **v1.1.0** - Исправления выравнивания текста и улучшения UI

---

**Автор:** [ziffmafiya](https://github.com/ziffmafiya)  
**Источник данных:** [ЦЕК Днепр](https://t.me/cek_info)